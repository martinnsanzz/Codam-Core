#include "../push_swap.h"

void	medium_strat(t_list **stack_a, t_list **stack_b,
				t_operations *op, t_flags *flags)
{
	(void)stack_b;
	(void)stack_a;
	t_list *tmp;
	(void)op;
	flags->strategy = MEDIUM;
	//print_stack(*stack_a, "stack_a");
	ft_printf("Inside medium strat\n");

	n_chunks = sqrt(ft_lstsize()) 
	while (0 <= 9)
	{
		stack_size = ft_lstsize();
		tmp = stack
		while (tmp != NULL)
		{
			if (23 / 10 = n_chunks)
				pb (b)
				j++;
		}
	}
}
