[project]
name = "Call_Me_Maybe"
version = "0.0.1"
requires-python = ">=3.13"
readme = "README.md"
description = """
This project creates a function calling tool that translates natural language
prompts into structured function calls
"""
authors = [
    {name = "masanz-s",email = "masanz-s@student.codam.nl"}
]
dependencies = [
    "numpy>=2.5.1",
    "pydantic>=2.13.4",
    "llm_sdk",
]

[dependency-groups]
dev = [
    "flake8>=7.3.0",
    "flake8-pyproject>=1.2.4",
    "mypy>=2.3.0",
    "pytest>=9.1.1",
    "uv>=0.12.1",
]

[tool.mypy]
exclude = [
    '^llm_sdk/',
    '^.venv'
]

[tool.flake8]
exclude = [
    ".venv/",
    "llm_sdk/"
]

[tool.uv.workspace]
members = ["llm_sdk"]

[tool.uv.sources]
llm-sdk = { workspace = true }
